export default {
  SNACKBAR: (state,  message) => state.snackbar = message,
  PAGE: (state, message) => state.page = message,
  ITEMS: (state, message) => state.items = message,
  ITEM: (state, message) => state.item = message,
  FILM: (state, message) => state.film = message,
  FASHION: (state, message) => state.fashion = message,
  ADVERTISING: (state, message) => state.advertising = message,
  SPORT: (state, message) => state.sport = message
}
